# Docker Container to Run Playbooks From This Repository

The Docker image in this directory packages the repository as a
portable control node: any playbook runs with only Docker and the
existing secret files on the host.

Primary documentation: [docs/architecture/concepts/runner-docker-image.md](../docs/architecture/concepts/runner-docker-image.md)
